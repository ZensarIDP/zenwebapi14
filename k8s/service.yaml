apiVersion: v1
kind: Service
metadata:
  name: zenwebapi14-service
  labels:
    app: zenwebapi14
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: zenwebapi14
